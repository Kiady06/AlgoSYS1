*Analyse du probleme*

**Entree** : 
    * Borne sup entier 
    * Born inf entier

**Sortie** : tableau contenant tous les nombres strictement entre les bornes inf et sup

**Conception** :
    - Si borne_sup = borne_max : null
    - Sinon :
        * initaliser un tableau vide
        * faire une boucle pour i = inf + 1 jusqu'a sup - 1 en stockant la valeur de i a chaques iterations dans un tableau

*Pseudo-code*
Debut
    Declarer bornSup : entier
    Declarer bornInf : entier
    Declarer rslt : tableau d'entier

    Lire born_sup
    Lire born_inf

    FONCTION getInterBeet(bornSup, bornInf)
    Si born_inf === born_sup
        RETURN null
    FIN SI
    POUR i = (born_inf + 1) jusqu'a bornSup - 1
        Ajouter i à la fin de rslt
    FIN POUR

    RETURN rslt
    FIN FONCTION
Fin